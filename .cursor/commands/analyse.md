# analyse

Create a new analysis of how the app works, and how the parts wire together. Pay particular attention to how the LLM integrates into the app, how the agent part works, and how the data is saved. Of paricular interest is what role the LLM works, what it does that can't be done without an LLM (or at least would be much harder). 

Save a file in /docs called analysis.md, overwriting any you find already there.

This command will be available in chat with /analyse