# Analyse

Create an analysis of how the app works, and how the parts wire together. Analyse how Langgraph helps make this an **agentic AI** app, how the _agent_ part works, and how the data is saved. Make sure the date is shown near the top of the analysis so I can make sure I'm looking at a relatively recent analysis

Pay attention also to the way the app has a 'memory' to help it recursively self-improve. How does that work, and how does it help the app 'learn'?

## LLMs
Pay particular attention to how the LLM integrates into the app, what it does that can't be done without an LLM (or at least would be much harder). To that end, always have a section called 'LLM use' showing exactly the parts of the app that use AIs/LLMs.

## LLM prompts
Always show how the LLM prompts are separated from the agent code. Show what each prompt controls, and how they feed into the actual agent code.

## Omit Notion
For the time being, leave Notion out of the analysis - I've disabled that part of things, and don't need it in the analysis.

## Avoid run instructions
Don't bother with instructions on how to run the app, etc. That belongs in the ReadMe.

## Save analysis
Save a file in my Obsidian vault (C:\Users\Lavelles\OneDrive\Documents\Obsidian Vault) in the 'Research Agent' folder, called 'Analysis.md', updating any you find already there. If there's no Obsidian vault, just save the file into the docs folder.

This command will be available in chat with /analyse