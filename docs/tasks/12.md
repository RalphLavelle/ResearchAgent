# New admin section
This site needs an admin section, under the route /admin/[pageTitle]. I need somewhere to manage the backend. First of all though I just need an index page there, linking to the reports page, which should be moved there. ANother page I need is 'Venues'.

# New venues page
Make a venues page in admin, showing all the venues records in page (50 max) lists. Link that page to the admin index as well. 