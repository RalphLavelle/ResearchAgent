# Images module broken?
Something has gone very wrong with the images module! Take a look at the terminal output, and you can see the API requests for images all showing 404s. What's going wrong? Why aren't they being found? Is the API broken or are those images really not in the images collection in the database?