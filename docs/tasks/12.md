I want to integrate Google Analytics 4 into this application.

Requirements:

1. Add Google Analytics 4 using the official gtag.js approach.

2. The Measurement ID should NOT be hardcoded.
   - Read it from an environment variable.
   - If no Measurement ID is configured, Analytics should silently do nothing.
   - Do not throw errors.

3. Ensure page navigation within the Angular SPA records page views correctly.
   - Listen for Angular Router navigation events.
   - Send a page_view event whenever navigation completes.
   - Do not send duplicate page views.

4. Do NOT add any custom GA events.
   I only want page_view tracking.

5. The URLs already encode the important information.
   For example:

   /
   /venue/the-triffid
   /venue/the-zoo
   /tag/jazz
   /tag/rock
   /event/12345

   These page views are sufficient because I can determine:
   - which venues are popular
   - which tags are popular
   - which events are popular

6. Make sure query strings are preserved in page views.
   For example:

   /search?q=beatles
   /?tag=jazz

7. Create a small Angular AnalyticsService responsible for:
   - injecting the GA script
   - initializing gtag()
   - sending page views
   - being easy to extend later

8. Keep all Analytics code isolated from the rest of the application.

9. Do not introduce any third-party Angular GA libraries.
   Use the official Google gtag implementation only.

10. Provide:
    - all code changes
    - any new files
    - environment variable changes
    - where the Measurement ID should be configured
    - instructions for testing using Google Analytics DebugView.

The implementation should be clean, minimal, production-ready, and easy to maintain.