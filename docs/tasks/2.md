# Deployment to Digital Ocean
Ok, MCP server installed. Now I want to deploy BOTH apps to Digital Ocean, the Angulkar UI and the Python agent.

I'll only be using this for a short while, so can oyu pick the cheapest possible plan  -my other projects are on a $5/month plan.

See if it's possible to keep storing the data (reports, xls, json, and above all, the images) in the same folder on the server, or if I need to move it to a different folder.

Can we now create the app spec?