# Tidy up
There are often venues with 0 events associated with them. Maybe that's because the events have been culled - not sure. In any case, it's distracting, and it pollutes the admin venues page with bad data. 

# New stage
Include a tidy-up stage at the end of each run with the task of deleting all the venues in the venues collection that have 0 events associated with them.