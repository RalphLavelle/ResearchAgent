# Redo the About page
At the moment the About page is more like a technical document, showing how the app uses LLMs and showing where it doesn't. BUt end users don;t care about where there is no LLM use - so redo the about page to only show the AI/LLM use, and remove the other boring stuff. If possible, phrase things in a funny way as well, to make AI/LLM fun. 