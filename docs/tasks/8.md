# Spotlight picks update
The spotlight picks section draws a random few events from the database, but it should only show ones that are within the window of events (e.g. 30 days) that the main listing is showing.

# Fix
Make the spotlight picks only choose events from the window that the rest of the events are showing from. Modify the database lookup to reflect this.