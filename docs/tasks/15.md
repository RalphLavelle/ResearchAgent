# More SPA-like
At the moment, when I click on a button to only show, say, 'rock' events, it works. Same with clicking on a venue name. However, these states of the app are not bookmarkable or saveable.

## Question
Is it possible to have the URL change so that clicking on a tag makes the app show a URL something like /tags/rock or venues/the-triffid? Would it also be possible to also work both ways, so that the app changes state to reflect the URL?