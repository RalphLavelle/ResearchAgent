# Notion integration

## Overview
I want to set up an integration between this app and my Notion. The idea is to save the research done with the app, currently being saved into a folder on the dektop, into a Notion page instead.

## In this app
I'll need to have Notion credentials or API Keys in the .env file to be able to connect to Notion. And obviously, code to make all this happen.

## In Notion
I've set up a connection called 'AI Agent' with which I want to connect this app to Notion. The page to save the research to already exists - it's called 'Research', under the 'AI' page.

## Task
Can you find out what exactly needs to happen for this app, running locally, to be able to write to a page in my Notion on a regular schedule, just like it's currebtly doing to save to the desktop. Ask me any questions that are necessary.