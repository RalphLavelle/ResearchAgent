# Front-end app
I need an Angular front-end app that the output can be used in. The idea is to allow people to click around within the list, drilling down into artists, venues, dates, etc.

## Rearrangement
Move the base spreadsheet and the run_log in /output to the /data folder. 

## Make the front-end app
Make an Angular app, entirely contained in a top-level folder called 'web', with just an index page. In that index component, have a sub-component called 'list', and make that the HTML that's generated from the spreadsheet, instead of the one in /output.

Remove the /output folder 