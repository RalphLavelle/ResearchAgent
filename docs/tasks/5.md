# Images question
Make sure that none of the events that don't actually have an image and instead use the default 🎸 emoji are not selected for the spotlight carousel. The whole point is to show nice images, not generic placeholders.

# About page
The about page text should be centred. As it is, it's slightly to the left, and doesn't occupy all its available space. 