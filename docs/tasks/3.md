# Target the venues more actively
Now that we have the venues in the database, I want to make sure we start looking more closely at what's on in those venues.

# Include a targeted venue search in the schedule
For each run, include a random (3-6) bunch of search queries in the form of "What's on in {venue name} in {location = Brisbane or GC}, Australia". Then read All the events possible as usual.

So - instead of the usual randomly generated seach queries, discard some and include the targeted venue ones. 