# No activity in the LLM layer
When no searches were done, or URLs crawled, as in the screenshot in this folder, I need to know why. If the tokens are exhausted, or the LLM API was unresponsive, whatveer the cause. At the moment, an uninformative message is all I get.

Modify the parts of the agent responsible for generating the search terms, or crawling the URLs, etc. to log the reason it couldn't carry out its task and put that in the report. It's vital I see that. 