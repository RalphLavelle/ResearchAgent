# Venue buttons
Now that the venues have been 'canonicalised', make it so that the venue (minus the trailing ",") shows as a light-coloured button. Whenever a venue button is clicked, only the events with the same venue id (hidden HTML field) should remain showing. Clicking again shows the whole list. 