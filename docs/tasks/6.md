# Expanded 'about' page
At the moment there is an about page that simply explains to the viewer what the site is about, and how it uses LLMs.

## Idea
I'd like a page where there is a more visual explanation of how LLMs are used in the site. If necessary, run the /analysis skill first to understand how the LLMs work in the app. Then make the page show each step, one-by-one, in hidden but expandable panels, that the app uses LLMs to achieve its goals.

## Diagrams
Is it possible to make accompanying diagrams and save them as .pngs?