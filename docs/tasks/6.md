# Venue deletion
In the admin section, venues page, when I delete a venue, it's often because it's completely the wrong sort of venue, and therefore the events it has are wrong too. So, instead of reassigning the events it has to another event, I want the option to delete those events too.

In the delete venue UI, update the UI to allow me to choose between reassigning the orphaned event(s) and deleting them.