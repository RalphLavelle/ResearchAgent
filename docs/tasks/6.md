# Full site crawl required?
In a recent task, I asked if the agent could be made to 'go deeper' and do a full crawl within sites it finds a slikely candidates for gigs/events, but I was told that "There is still no full site crawler (only DuckDuckGo + normalisation)"

Can we bake in a full site crawl into the agent? What libraries would it need to do that. It's vital the that agent be able to dig deep into websites in order to compile a list of individual gigs/events...

# Wrong sort order
The output on the Notion page should show the earliest soonest

# Images should be shown as actual images
The images are just 'shown'