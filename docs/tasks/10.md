# Random images
The site lack visual appeal! Make a section at the top of the page, between the header and the list itself, to hold a grid of 4 random (from the list) event cards, which should be responsive. The narrowest screen should show only one image, but at widest all four can show. There should be arousel arrow keys to allow navigating through all 4 in cases where less than 4 show.

## Individial cards
Each event card should show the image, with event name, venue, and date showing underneath, linking the image and title to the external url.

## Styling
Make sure there is a good bit of space between the heading and the carousel and he lis. Things are a bit cramped at the moment.

## Refreshing
Each time the page is loaded make sure the images are refreshed.