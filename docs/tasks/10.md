# Comments and suggestions
One way to improve the project is to allow users to add comments and suggestions. I'll need a new collection for this - with fields: comment, name, date. Instead of my name at the footer, make a button to add a comment. When a user clicks the button, a modal should appear with a text area and a submit button. When the user submits the comment, it should be added to the collection and the page should thank the user. They can then close the modal manually