# Migrations
I have a couple of database schema changes to make, so I'd like it if they could be organised into a folder called migrations that will run the next time there is a scheduled run. That was they will run on the live database too, when I deploy these changes. Having them as separate files then means I can remove them once they've been run. 

## Remove 'poster_url'
In the events collection, plesase remove the 'poster_url' field. The linked image will have that if it even needs it.

## Move 'location' to 'venues'
Remove the location field from events, find the relevant venue record for that event, and create a new field in venues called 'location'. Don't't worry about overwriting where there are multiple of the same venue/location - they should be safe overwrites of the same value.

## Update the front-end
In the UI show the venue as {name}, {location} in the button.

In the admin section, venues page, update the list to show the location.