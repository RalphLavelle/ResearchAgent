# Event link policy
Due to a recent change, the Date column only shows for the first event on a particular date. The problem with this is that many events now have no link to the actual web page where you can find out more info.

# Link from the event name
Change the table so the link to the actual exturnal url of an event is the name of the event. This means that the old javascript function where clicking on the name of an event sometimes (if there were more of that event in the table) showed a UI listing more vents of this name in the table. Get rod of that UI and jist link the name to the external URL.

Then remove the link from the date column.