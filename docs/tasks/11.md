# Advanced logging of the three LLM steps
In order to understand the app, I'd like more advanced logging, especially around the steps that involve the LLM since they can be hard to understand.

From now on, instead of a single run_log, I'd like a markdown file produced for each run, called 'Run_[datetime]'.md, where the [datetime] is the timestamp (AEST) in a standard format. In that run file, there should be 3 sections: 

1. A section called 'Searches'

In the analysis file [C:\Users\Lavelles\OneDrive\Documents\Obsidian Vault\Research Agent\analysis.md] in the Pipelines steps, no.1, Plan, it says 'The LLM returns `PlanQueries` — a list of up to `MAX_SEARCH_QUERIES` (default 8) distinct web search queries.' Save those queries into the run file in this section. I need to see what's being generated.

2. Search and crawl
Save a list of the URLs, grouped by same-host, the agent decides to crawl.

3. In the Normalize step ('4. Normalize'), save the list of `Resource` Pydantic models, serialised if necessary, along with their http. I wantto be able to analyse which url generates each resource the LLM considers a valid event.