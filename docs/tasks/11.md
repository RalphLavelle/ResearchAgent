Can you check the logic behind the selction of the Spotlight Picks carousel? It's supposed to only show events which have a non-generic image, but there are several which have the generic image showing. It should filter at the database level. Investigate why there are events with no image of their own being selected in the carousel.