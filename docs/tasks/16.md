# About page
Make an about page, and create some copy about hos this site uses an agent and LLMs, etc., to explain to people how it works. If necessary, make a common layout page so that both the index and about (and any future pages) can share the same layout (header menu, footer, etc.)

In the header menu, make one of the phony links link to the new About page, and get rid of the rest.