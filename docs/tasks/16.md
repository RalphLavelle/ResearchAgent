# Make the scheule interval .env configurable
At the moment the whole agent schedule runs on an hourly (or miutely) schedule as dictated by a config file in the topics folder. THis should be changed to only ever an hourly value, and should be in the .env file. THis is the only place this should be set, nowhere else.

## Tidy up
When this has been done, pls remove the schedule.yaml file(s).