# Admin are slowness
In the admin area, the venues page takes a long time to load the venues, just showing "Loading venues..." for a long time. When I go to Delete a venue, the (same) lookup, to get all (bar the one I've chosen) the venues to populate the modal, takes a long time too.

Is there a missing index? How can we speed up the venues fetching from the database?

Investigate, if necessary, by using the live Mongo URL (.env key: MONGODB_URI).