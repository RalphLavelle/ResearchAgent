# Admin section schedule trigger
Make a button in the admin section, reports page, that triggers a run of the agent scheduler, the same one that runs when we enter the command:
.\venv\Scripts\python.exe -m agent run-once
into the terminal.

It should do exactly what that command does. It should have a spinner logo showing that it's running, then show completed when it's finished.