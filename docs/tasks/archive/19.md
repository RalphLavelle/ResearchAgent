# Filter unwanted events
Sometimes, despite instructions in the agent's curation step to exclude certain types of events, they still get through and end up in the final spreadsheet. Once n the spreadhseet they end up in the events JSON file derived form that, and from there in the final web frontend.

I'd like a step added, maybe as part of the dedupe, because it's similar in nature, that can perform a last minute pruning of events that should not be in the list. An exclusion list should be used to help prompt an LLM query to see if it thinks any of the events match that filter. The list should be easily accessible, i.e. configurable, by me, the develop, so I don;t have to go deep into the Pythin just to find it. The agent can read it and filter the list to make sure it doesn't have any events that match the types in the exlusion list.

# Example
Use 'drag events' or 'drag' as an example of something that should be on the exclusion list since these are not usually musical.

# Update
This is still not working - please do a run and confirm that ALL drag-related events have been pruned.

No need for two steps, as the exclusion_prune.py file seems to imply. The '**Spreadsheet prune**' step 2 looks like it would be once-off and we can simplify the code to remove that since I can manually remove any events I don't want myself. I only need to have a step to do this that fits into the agent pipeline that gets run each and every run. 