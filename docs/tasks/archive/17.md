## Colourful background
Use the image at /web/public/bg.jpg as a full-width background image, suitably transparent.  

## Events table
In the events table, get rid of the gradient background for the column headers. Just have faint outlines for those cells.

# Subheadline
Replace "What's on in your town." with "What's on in Brisbane and the Gold Coast"

Remove the 'Upcoming Live Music Events' headline

# Date column grouping
In the Events table, only show the date when it's the first event for that date.