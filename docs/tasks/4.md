Recently the background colour was changed to match the background colour of the main Gigsorooni image, but it seems to have only been done on the about page and the admin pages. The home page still has a slightly visible difference between the background colour of the page and the background colour of the image itself. Fix this