# Move to MongoDB
We're currently using a spreadsheet to store the data. This is not ideal, as it's not very scalable, and not very easy to query. We need to move to a MongoDB database.

# MongoDB database schema
The database will be topic-based, and should use the 'db' property in the topic object to create the database name. This should be a new database, and should be created in the MongoDB Atlas cloud. This database should be created with the following collections:

# New topic property
I removed the 'data-dir' property and added a new one called 'db'. This is the database name

# Migration
Move the events.json to the events collection. Also move the images folder to the images collection. They'll have to be stored as base64 or blobs - can you decide? Then link them back to the events. Remove the spreadsheet, the json, and the images folder from the data/{topic-name} folder. Update the Python code that does all this too. From now on everything goes through the database. THe front-end should also be updated to use the new database.

# Skill
All of this should be used to update the 'topic-creator' skill.