# Some tidy up

## Remove fields
Taking /data/Run_2026-05-22_15-27-19_AEST.md as an example, in the '3. Normalize' section it shows json objects it compiles. Problem is there are unnecessary fields still being compiled which are never used. Please remove the following fields:
* resource_type
* price
* participatory

Follow the chain all through the compilation of these json objects are update all the code involved in generating these fields. Remove any reference to those fields from any of the LLM prompts too.